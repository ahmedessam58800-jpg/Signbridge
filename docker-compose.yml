services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: signbridge
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - signbridge-postgres:/var/lib/postgresql/data

volumes:
  signbridge-postgres:
